#include<iostream>
#include<algorithm>
using namespace std;

int main() {
    int m;
    long long n, a, d;
    cin >> n >> m >> a >> d;
    int opens = 0;
    int curOpenTime = -1;
    int nextEm = a;
    int nextCl;
    int clC
    cin >> nextCl;
    curOpenTime = !(nextEm<nextCl)?nextCl:nextEm;
    while(curOpenTime < nextEm) {
        if(
        nextCl += a;

    }
    for(int i = 1; i < m; i++) {
        
    }
}

